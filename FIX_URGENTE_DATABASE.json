# 🚨 FIX URGENTE: database.json contiene dati sensibili!

## ⚠️ PROBLEMA TROVATO

Il file `data/database.json` è **già tracciato da Git** e contiene:
- ❌ Nomi reali di utenti
- ❌ Indirizzi reali
- ❌ Email reali (es. sigorn@hotmail.it)
- ❌ Telefoni reali
- ❌ Dati spedizioni

**Questo è un problema di sicurezza GDPR!**

---

## 🔧 FIX IMMEDIATO (ESEGUI SUBITO)

### Passo 1: Rimuovi database.json da Git

```bash
# Rimuovi dal tracking Git (ma mantieni il file locale)
git rm --cached data/database.json

# Verifica che sia stato rimosso
git status
# Dovresti vedere: "deleted: data/database.json"
```

### Passo 2: Verifica .gitignore

Il file `.gitignore` è già stato aggiornato per includere `data/database.json`.

Verifica:
```bash
# Windows CMD
findstr /i "database.json" .gitignore

# Dovrebbe mostrare: data/database.json
```

### Passo 3: Commit la rimozione

```bash
git add .gitignore
git commit -m "security: remove database.json from tracking (contains sensitive data)"
```

### Passo 4: Verifica che non venga più tracciato

```bash
git status
# data/database.json NON dovrebbe apparire
```

---

## ✅ DOPO IL FIX

Il file `data/database.json`:
- ✅ **Rimane sul tuo computer** (non viene cancellato)
- ✅ **Non viene più tracciato da Git**
- ✅ **Non verrà committato in futuro**
- ✅ **Non apparirà su GitHub**

---

## 🔒 VERIFICA FINALE

Prima di fare push, verifica:

```bash
# Verifica che database.json NON sia tracciato
git ls-files | findstr /i "database.json"
# Dovrebbe essere VUOTO (nessun output)

# Verifica che .env.local NON sia tracciato
git ls-files | findstr /i "\.env"
# Dovrebbe mostrare solo file .md e .txt (documentazione)
```

---

## 📋 COMANDI COMPLETI PRE-PUSH

```bash
# 1. Rimuovi database.json (se non l'hai già fatto)
git rm --cached data/database.json

# 2. Aggiungi .gitignore aggiornato
git add .gitignore

# 3. Commit
git commit -m "security: protect sensitive files (database.json, env files)"

# 4. Verifica stato
git status

# 5. Se tutto OK, push
git push origin master
```

---

## 🛡️ RACCOMANDAZIONE

**Considera di rendere la repo PRIVATA:**

1. Vai su: `https://github.com/gdsgroupsas-jpg/spediresicuro`
2. Settings → Change repository visibility
3. Clicca "Make private"

**Perché:**
- Protegge meglio i dati sensibili
- Anche se database.json è ignorato, altri file potrebbero contenere info
- Gratuito per account personali

---

**🔒 ESEGUI IL FIX PRIMA DI FARE PUSH!**

