name: Deploy Automation

on:
  push:
    branches: [ master ]
    paths:
      - 'services/automation/**'
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Deployment
        run: |
          echo "Deploy services/automation su Railway"
          echo "Railway fa auto-deploy da GitHub, questo workflow e solo per notifiche"
