# 🚀 COMANDI RAPIDI - Lavoro Remoto

## 📥 SCARICARE PROGETTO (Prima Volta a Casa)

cd C:\Users\TuoNome\Documents
git clone https://github.com/gdsgroupsas-jpg/spediresicuro.git
cd spediresicuro
npm install
npm run dev

---

## 🔄 SINCRONIZZARE (Ogni Volta)

### Scaricare Modifiche (Inizio Sessione):
cd C:\percorso\spediresicuro
git pull origin master
npm install
npm run dev

### Salvare Modifiche (Fine Sessione):
git add .
git commit -m "Descrizione modifiche"
git config user.name
git push origin master

---

## ⚠️ VERIFICA ACCOUNT (Prima di Push)

git config user.name
# Deve essere: gdsgroupsas-jpg
# Se non lo è:
git config user.name "gdsgroupsas-jpg"

---

## 📋 STATO PROGETTO

git status          # Vedi modifiche
git log --oneline   # Vedi ultimi commit

---

## 🆘 RISOLVI PROBLEMI

git pull origin master    # Scarica modifiche
git add .                 # Aggiungi tutto
git commit -m "Messaggio" # Salva
git push origin master    # Carica

