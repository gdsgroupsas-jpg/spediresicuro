[build]
builder = "DOCKERFILE"
dockerfilePath = "automation-service/Dockerfile"
dockerContext = "automation-service"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
